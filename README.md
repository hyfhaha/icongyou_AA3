# 学习类后端（完整版）
此后端为基于你提供的数据库 DDL 与功能需求生成的完整骨架（Node.js + Express + Sequelize + MySQL）。
包含模型、控制器、路由、上传、AI问答接口示例、视图迁移与种子数据、OpenAPI 与 Postman collection 示例。

快速启动：
1. 修改 .env (数据库连接等)
2. npm install
3. 执行 migrations 下的 SQL 脚本到你的 MySQL 数据库
4. npm run dev
